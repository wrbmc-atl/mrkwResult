<Window x:Class="Views.WindowEditRaceMaster"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks" 
        xmlns:local="clr-namespace:Views"
        xmlns:viewmodels="clr-namespace:ViewModels"
        xmlns:converters="clr-namespace:mrkwResult.Common"  
        mc:Ignorable="d"
        FontSize="20"
        FontFamily="07やさしさゴシック"
        Title="レースマスタ編集_WindowEditRaceMaster"
        Height="680" Width="960"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisiblityConverter"/>
        <converters:BooleanToCharConverter x:Key="BooleanToCharConverter"/>
        <Style TargetType="TextBlock">
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="Background" Value="LightBlue"/>
                </Trigger>
            </Style.Triggers>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Button">
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="Background" Value="LightBlue"/>
                </Trigger>
            </Style.Triggers>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Width" Value="270"/>
            <Setter Property="FontSize" Value="16"/>
        </Style>
        <Style TargetType="CheckBox">
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="Background" Value="LightBlue"/>
                </Trigger>
            </Style.Triggers>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="RadioButton">
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="Background" Value="LightBlue"/>
                </Trigger>
            </Style.Triggers>
            <Setter Property="FontSize" Value="16"/>        
        </Style>
        <Style TargetType="DatePicker">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="FontSize" Value="20"/>
        </Style>
        <Style TargetType="StackPanel">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <GroupBox Header="検索" Grid.Row="0" Margin="0,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="スタート:" Grid.Column="0" VerticalAlignment="Center" Margin="5" Width="80"/>
                <ComboBox x:Name="cmbStart_Search" Grid.Column="1" SelectionChanged="SelectionChanged_SearchSTART_CD" Width="300"
                        ItemsSource="{Binding obcStartCourse, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                        SelectedValuePath="COURSE_CD" DisplayMemberPath="COURSE_NM_DISP">
                </ComboBox>
                <TextBlock Text="　ゴール:" Grid.Column="2" VerticalAlignment="Center" Margin="5" Width="80"/>
                <ComboBox x:Name="cmbGoal_Search" Grid.Column="3" SelectionChanged="SelectionChanged_SearchGOAL_CD" Width="300"
                        ItemsSource="{Binding obcGoalCourse, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                        SelectedValuePath="COURSE_CD" DisplayMemberPath="COURSE_NM_DISP">
                </ComboBox>
                <Button Content="検索" Click="Click_Search" Grid.Column="4" Margin="5"/>
            </Grid>
        </GroupBox>

        <GroupBox Header="コース情報" Grid.Row="1" Margin="0,10,0,0">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="スタート:" Margin="5"/>
                    <TextBox Text="{Binding RaceInfo.START_NM, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Margin="5" MinWidth="240" IsEnabled="False" Focusable="False"/>
                    <TextBlock Text="　ゴール:" Margin="5"/>
                    <TextBox Text="{Binding RaceInfo.GOAL_NM, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Margin="5" MinWidth="240" IsEnabled="False" Focusable="False"/>
                </StackPanel>
                
                <TextBlock Text="表示順:" Grid.Row="3" Grid.Column="0" Margin="5"/>
                <TextBox Text="{Binding RaceInfo.DISPORDER, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Grid.Row="3" Grid.Column="1" Margin="5" TextAlignment="Right" IsEnabled="False" Focusable="False"/>

                <CheckBox Content="逆走" IsChecked="{Binding RaceInfo.REVERSE_FLG, Converter={StaticResource BooleanToCharConverter}}" Grid.Row="4" Grid.Column="1" Margin="5"/>
                
                <TextBlock Text="アイテム:" Grid.Row="5" Grid.Column="0" Margin="5"/>
                <TextBox Text="{Binding RaceInfo.ITEM, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Grid.Row="5" Grid.Column="1" Margin="5"/>

                <TextBlock Text="ショートカット1:" Grid.Row="6" Grid.Column="0" Margin="5"/>
                <TextBox Text="{Binding RaceInfo.SHTC1, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Grid.Row="6" Grid.Column="1" Margin="5"/>

                <TextBlock Text="画像パス1:" Grid.Row="7" Grid.Column="0" Margin="5"/>
                <TextBox Text="{Binding RaceInfo.SHTC_PIC1, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Grid.Row="7" Grid.Column="1" Margin="5"/>

                <TextBlock Text="ショートカット2:" Grid.Row="8" Grid.Column="0" Margin="5"/>
                <TextBox Text="{Binding RaceInfo.SHTC2, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Grid.Row="8" Grid.Column="1" Margin="5"/>

                <TextBlock Text="画像パス2:" Grid.Row="9" Grid.Column="0" Margin="5"/>
                <TextBox Text="{Binding RaceInfo.SHTC_PIC2, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Grid.Row="9" Grid.Column="1" Margin="5"/>

                <TextBlock Text="備考:" Grid.Row="10" Grid.Column="0" VerticalAlignment="Top" Margin="5"/>
                <TextBox Text="{Binding RaceInfo.REMARK, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Grid.Row="10" Grid.Column="1" Height="80" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="5"/>

            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="更新" Click="UpdateButton_Click" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>